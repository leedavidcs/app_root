# Migration `20200518212647-add-time-in-force-to-order`

This migration has been generated by leedavidcs at 5/18/2020, 9:26:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "TimeInForce" AS ENUM ('Day', 'GTC', 'OPG', 'CLS', 'IOC', 'FOK');

ALTER TABLE "public"."Order" ADD COLUMN "timeInForce" "TimeInForce" NOT NULL DEFAULT 'Day',
ALTER COLUMN "status" SET DEFAULT 'Open',
ALTER COLUMN "type" SET DEFAULT 'Market';

ALTER TABLE "public"."Transaction" ALTER COLUMN "status" SET DEFAULT 'PENDING';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200518042708-rename-stock-portfolio-balance-to-market-value..20200518212647-add-time-in-force-to-order
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -74,20 +74,30 @@
   Open
   Closed
 }
+enum TimeInForce {
+  Day
+  GTC
+  OPG
+  CLS
+  IOC
+  FOK
+}
+
 model Order {
   id               String         @id @default(cuid())
   stockPortfolio   StockPortfolio @relation(fields: [stockPortfolioId], references: [id])
   stockPortfolioId String
   ticker           String
   quantity         Int
-  type             OrderType
+  type             OrderType      @default(Market)
   side             OrderSide
   status           OrderStatus    @default(Open)
   limitPrice       Float?
   stopPrice        Float?
   avgFilledPrice   Float?
+  timeInForce      TimeInForce    @default(Day)
   createdAt        DateTime       @default(now())
   filledAt         DateTime?
   cancelledAt      DateTime?
   failedAt         DateTime?
```



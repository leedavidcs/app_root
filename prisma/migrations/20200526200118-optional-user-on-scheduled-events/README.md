# Migration `20200526200118-optional-user-on-scheduled-events`

This migration has been generated by leedavidcs at 5/26/2020, 8:01:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Order" ALTER COLUMN "status" SET DEFAULT E'Open',
ALTER COLUMN "timeInForce" SET DEFAULT E'Day',
ALTER COLUMN "type" SET DEFAULT E'Market';

ALTER TABLE "public"."ScheduledEvent" DROP CONSTRAINT IF EXiSTS "ScheduledEvent_userId_fkey",
ALTER COLUMN "userId" DROP NOT NULL;

ALTER TABLE "public"."Transaction" ALTER COLUMN "status" SET DEFAULT E'PENDING';

ALTER TABLE "public"."ScheduledEvent" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200525222437-order-event-model..20200526200118-optional-user-on-scheduled-events
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -196,17 +196,17 @@
   Daily
 }
 model ScheduledEvent {
-  id         String   @id @default(cuid())
-  user       User     @relation(fields: [userId], references: [id])
-  userId     String
+  id         String      @id @default(cuid())
+  user       User?       @relation(fields: [userId], references: [id])
+  userId     String?
   recurrence Recurrence?
   days       Day[]
-  hour       Int      @default(0)
-  minute     Int      @default(0)
+  hour       Int         @default(0)
+  minute     Int         @default(0)
   interval   Int?
-  next       DateTime @default(now())
+  next       DateTime    @default(now())
 }
 enum StockPortfolioEventType {
   DataRetrieved
```



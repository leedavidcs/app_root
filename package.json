{
  "name": "app_root",
  "version": "0.0.1",
  "description": "Application root, for development purposes.",
  "main": "index.js",
  "scripts": {
    "build": "echo $(date +%s) && docker-compose build --build-arg CACHE_BUST_FILES_UPDATED=$(date +%s) --pull server",
    "stop": "docker-compose down",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "docker-compose up",
    "start:deps": "docker-compose up server-dependencies"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mua-inc/app_root.git"
  },
  "author": "leedavidcs",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mua-inc/app_root/issues"
  },
  "homepage": "https://github.com/mua-inc/app_root#readme",
  "dependencies": {}
}

import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks";
import { EditableText } from ".";

<Meta title="general/inputs/editable-text" component={EditableText} />

# EditableText

## Description
A text-input that appears as a normal UI text, but transforms into a text-input field when it is
focused. See [blueprint.js](https://blueprintjs.com/docs/#core/components/editable-text).

## Props
<Props of={EditableText} />

## StandardStory
<Preview>
	<Story name="standard">
		<EditableText />
	</Story>
</Preview>

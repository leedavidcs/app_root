import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks";
import { useState } from "react";
import { StandardStory } from "./stories";
import { KebabMenu } from ".";

<Meta title="general/kebab-menu" component={KebabMenu} />

# KebabMenu

- [Table of Contents](#kebabmenu)
  * [Description](#description)
  * [Props](#props)
  * [StandardStory](#standardstory)
    + [Source](#source)

## Description
Kebab menu, that is meant to display actions that a user can take.

## Props
<Props of={KebabMenu} />

## StandardStory
<Preview>
	<Story name="standard">
		<StandardStory />
	</Story>
</Preview>

### Source
```tsx
import { IKebabMenuOption, KebabMenu } from "@/client/components/kebab-menu.component";
import { action } from "@storybook/addon-actions";
import Faker from "faker";
import { range } from "lodash";
import React, { FC, useMemo } from "react";
import { useStyles } from "./styles";

Faker.seed(1);

const DATA_SIZE = 5;

export const StandardStory: FC = () => {
	const classes = useStyles();

	const options: readonly IKebabMenuOption[] = useMemo(() => {
		return range(DATA_SIZE).map((__, i) => ({
			text: Faker.lorem.word(),
			onClick: action(`onClick${i}`)
		}));
	}, []);

	return (
		<div className={classes.root}>
			<KebabMenu options={options} />;
		</div>
	);
};
```

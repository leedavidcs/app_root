import { action } from "@storybook/addon-actions";
import { boolean } from "@storybook/addon-knobs";
import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks";
import Faker from "faker";
import { range } from "lodash";
import { StockPortfolioList } from ".";
//
Faker.seed(1);

<Meta title="page-parts/users/[userId]/stock-portfolios/stock-portfolio-list" component={StockPortfolioList} />

# StockPortfolioList

- [Table of contents](#stockportfoliolist)
  * [Description](#description)
  * [Props](#props)
  * [StandardStory](#standardstory)
    + [Source](#source)

## Description
A list that displays the stock portfolios that the user has created.

## Props
<Props of={StockPortfolioList} />

export const stockPortfolios = range(10).map(() => ({
	id: Faker.random.uuid(),
	name: Faker.name.firstName(),
	updatedAt: Faker.date.recent().toDateString()
}));

## StandardStory
<Preview>
	<Story name="standard">
		<div style={{ width: 500 }}>
			<StockPortfolioList
				loading={boolean("loading", false)}
				onClickOpen={action("onClickOpen")}
				onClickDelete={action("onClickDelete")}
				stockPortfolios={stockPortfolios}
			/>
		</div>
	</Story>
</Preview>

### Source
(see code in story)

import { GetOneStockPortfolioMock } from "@/client/graphql/mocks/get-one-stock-portfolio.mock";
import { boolean } from "@storybook/addon-knobs";
import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks";
import { StockPortfolioDisplay } from ".";

<Meta
	title="page-parts/stock-portfolios/[stockPortfolioId]/stock-portfolio-display"
	component={StockPortfolioDisplay}
/>

# StockPortfolioDisplay

## Description
A stock-portfolio display, which displays 3rd-party data on a data-grid, based on selected tickers
and configuration.

## Props
<Props of={StockPortfolioDisplay} />

## StandardStory
export const stockPortfolio = GetOneStockPortfolioMock.result.data.stockPortfolio;

<Preview>
	<Story name="standard">
		<div style={{ maxWidth: 1280 }}>
			<StockPortfolioDisplay
				loading={boolean("loading", false)}
				stockPortfolio={stockPortfolio}
			/>
		</div>
	</Story>
</Preview>

### Source
(see code in story)

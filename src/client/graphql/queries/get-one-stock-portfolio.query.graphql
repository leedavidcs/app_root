query GetOneStockPortfolio($where: StockPortfolioWhereUniqueInput!) {
	stockPortfolio(where: $where) {
		id
		name
		headers {
			name
			dataKey
			width
			frozen
			resizable
		}
		tickers
		createdAt
		updatedAt
		user {
			id
			username
		}
		data
	}
}

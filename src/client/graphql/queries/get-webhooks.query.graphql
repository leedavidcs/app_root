query GetWebhooks(
	$first: Int
	$skip: Int
	$after: WebhookWhereUniqueInput
	$where: WebhookWhereInput
) {
	webhooks(
		first: $first
		skip: $skip
		after: $after
		where: $where
	) {
		id
		name
		type
		url
		createdAt
	}
	count: webhookCount(where: $where)
}
